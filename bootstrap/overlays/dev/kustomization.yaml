apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# dev is the default environment baked into ../../base — the child Applications
# already point at each component's overlays/dev. Nothing to override here.
resources:
  - ../../base
