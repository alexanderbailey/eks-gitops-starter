apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# dev: run an ephemeral in-cluster Postgres and hand db-service static creds.
namespace: db-service
resources:
  - ../../base
  - db-credentials.yaml
  - db-config.yaml
  - postgres.yaml
