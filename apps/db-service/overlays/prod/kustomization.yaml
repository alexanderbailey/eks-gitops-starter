apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# prod: credentials via ESO from RDS/Secrets Manager; no in-cluster Postgres.
namespace: db-service
resources:
  - ../../base
  - externalsecret.yaml
  - db-config.yaml
