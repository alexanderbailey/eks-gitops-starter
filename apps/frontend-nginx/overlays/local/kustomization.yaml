apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# local (kind): same as dev — nginx IngressClass, no public host or TLS. Reach
# it via the ingress-nginx NodePort that kind-up maps to localhost:8080.
resources:
  - ../dev
