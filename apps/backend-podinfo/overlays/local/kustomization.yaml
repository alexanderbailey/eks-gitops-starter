apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# local (kind) is identical to dev for this app — a single replica of a public
# image. Kept as its own overlay so it can diverge later without touching dev.
resources:
  - ../dev
