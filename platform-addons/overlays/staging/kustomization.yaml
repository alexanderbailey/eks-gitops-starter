apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# staging: real ACME certs (Let's Encrypt STAGING endpoint — untrusted certs, no
# rate limits, safe to iterate). Ingress is ALB (from Terraform); self-signed
# issuer from base stays available too.
resources:
  - ../../base
  - cluster-issuer-letsencrypt.yaml
